{
  "company": "Ritivel",
  "repo": "regulus-eval",
  "metrics": [
    "product_coverage",
    "promises_risk",
    "level_precision",
    "traceability_latency"
  ],
  "failure_modes": [
    "product_drift",
    "promises_gap",
    "level_misroute",
    "traceability_blindspot"
  ],
  "domain_archetypes": [
    {
      "name": "product evidence replay",
      "trigger": "product signal changes while promises context is stale",
      "expected": "block release until cited evidence is regenerated"
    },
    {
      "name": "promises boundary probe",
      "trigger": "promises handoff crosses a policy or trust boundary",
      "expected": "route to reviewer with evidence packet"
    },
    {
      "name": "level regression harness",
      "trigger": "level behavior regresses against the last accepted fixture",
      "expected": "open a regression issue with trace and benchmark delta"
    },
    {
      "name": "traceability operator packet",
      "trigger": "traceability output needs a human-readable audit packet",
      "expected": "accept only if decision claims cite fixture evidence"
    }
  ],
  "minimum_cases": 144,
  "requires_evidence_markers": true
}